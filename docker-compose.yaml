services:
  klog-dashboard:
    image: ghcr.io/muecahit94/klog-dashboard:latest
    build: .
    container_name: klog-dashboard
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - KLOG_DATA_DIR=/data
      - HOSTNAME=0.0.0.0
    volumes:
      - ./data:/data
